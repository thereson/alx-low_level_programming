#include <main.h>
/**
 *print number from 0-9
 *int declare variable
 *Return 0 (success)
 */
void print_numbers(void)
{
	int x;
	
	for(x = 0; x <= 9; x++)
	{
		_putchar(x);
	}
	        _putchar(\n);
		return (0)
}
